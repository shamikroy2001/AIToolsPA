# Personal Assistant backend

Python FastAPI API and Arq worker (D1 staging freeze). Install from this directory:

```bash
python -m pip install -e ".[dev]"
python -m uvicorn app.main:app --reload --port 8000
```

Legacy SQLite CLI: `python project.py` (not the SaaS product).
